version: "3.8"

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: z-image-turbo-web
    ports:
      - "18080:80"
    restart: always
    environment:
      - NODE_ENV=production
